# @package _global_
defaults:
  - override /dataset: kmnist
  - override /trafo: simple_trafo
  - override /dip: kmnist_unet
  - override /inference: kmnist_inference
  - override /mll_optim: kmnist_mll_optim

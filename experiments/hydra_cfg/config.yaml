defaults:
  - dataset: kmnist
  - trafo: simple_trafo
  - dip: unet
  - mll_optim: mll_optim
  - inference: kmnist_inference
seed: 1
num_images: 1
use_double: False
load_dip_models_from_path: null
hydra:
  run:
    dir: outputs/${now:%Y-%m-%dT%H:%M:%S.%fZ}
  sweep:
    dir: multirun/${now:%Y-%m-%dT%H:%M:%S.%fZ}

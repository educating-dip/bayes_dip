defaults:
  - base_inference

add_image_noise_correction_term: False
low_rank_neural_basis_expansion:
  low_rank_rank_dim: 500
  batch_size: 16
  use_cpu: True
save_samples_chunk_size: 1024
verbose: False
cov_obs_mat:
  batch_size: 16
sampling:
  batch_size: 16
  cg_preconditioner:
    low_rank_rank_dim: 400
    batch_size: 16

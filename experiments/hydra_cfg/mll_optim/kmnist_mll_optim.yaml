defaults:
  - base_mll_optim

iterations: 1500
num_probes: 10
linear_cg:
  max_iter: 50
  preconditioner:
    low_rank_rank_dim: 200
    oversampling_param: 10
    batch_size: 32
predcp:
  num_samples: 500
  scale: 1.

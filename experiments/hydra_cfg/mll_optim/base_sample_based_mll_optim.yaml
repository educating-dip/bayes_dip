iterations: 5
num_samples: 64
noise_variance_init_value: 1
sampling:
  batch_size: 8
  use_conj_grad_inv: True
  cg_kwargs:
    rtol: 1e-3
    max_niter: 150
    ignore_numerical_warning: False
    use_log_re_variant: False
use_sample_then_optimise: False
use_preconditioner: True
preconditioner:
  name: low_rank_eig
  # shared:
  low_rank_rank_dim: 400
  batch_size: 8
  # used by low_rank_eig:
  oversampling_param: 5
  # used by incomplete_chol:
  approx_diag_num_samples: 200
  load_state_dict: null
  load_approx_basis: null
activate_debugging_mode: False
debugging_mode_kwargs:
  verbose: True
  loglikelihood_kwargs:
    num_samples: 64
    noise_x_correction_term: False
    verbose: False
    return_patch_diags: False
    unscaled: False
    patch_kwargs:
      batch_size: 8
      patch_size: 1
      patch_idx_list: null
      reweight_off_diagonal_entries: True
log_path: ./
init_load_path: null

defaults:
  - base_mll_optim

iterations: 3000
num_probes: 10
scheduler:
  use_scheduler: True
linear_cg:
  max_iter: 50
  use_log_re_variant: True
  preconditioner:
    name: incomplete_chol
    low_rank_rank_dim: 100
    batch_size: 32
    approx_diag_num_samples: 100
predcp:
  num_samples: 20
  scale: 1.
use_linearized_weights: True
